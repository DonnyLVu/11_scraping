const numbers = (str) => {

  if (str === 'One') {
    return 1;

  } if (str === 'Two') {
    return 2;

  } if (str === 'Three') {
    return 3;

  } if (str === 'Four') {
    return 4;

  } else return 5;
};

module.exports = numbers;
